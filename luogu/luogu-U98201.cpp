//
// Created by byron on 2019/12/10.
//
#include <iostream>
#include <cstring>
using namespace std;
int n,k,sum = 0;
int main() {
    cin >> n >> k;
    int steup[k + 5];
    memset(steup,0, sizeof(steup));
    for (int i = 0; i < n; ++i) {
        if (i < k)
    }
    cout << steup[I] << endl;
    return 0;
}